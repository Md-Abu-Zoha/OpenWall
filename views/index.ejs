<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>OpenWall</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div style="margin-bottom: 30px;">
      <h1><strong>All Chats</strong></h1>
      <hr />
      <form method="get" action="/chats/new" style="display: inline-block; margin-top: 20px;">
        <button class="btn btn-outline-success" type="submit">
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-plus-circle" viewBox="0 0 16 16" style="margin-right: 5px; vertical-align: middle;">
            <path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14zm0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16z"/>
            <path d="M8 4a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 8 4z"/>
          </svg>
          New Chat
        </button>
      </form>
    </div>
    <div class="chat-container">
      <% if(chats.length === 0) { %>
        <div style="background: rgba(255, 255, 255, 0.95); padding: 40px; border-radius: 16px; text-align: center; color: #6c757d;">
          <p style="font-size: 1.2rem; margin: 0;">No chats yet. Be the first to post!</p>
        </div>
      <% } else { %>
        <% for(let chat of chats) { %>
        <div class="chat card">
          <div class="card-header">
            <strong>From: <i><%= chat.from %></i></strong>
          </div>
          <div class="msg card-body">
            <p class="card-text"><%= chat.message %></p>
            <div class="buttons">
              <form method="get" action="/chats/<%= chat._id %>/edit" style="display: inline;">
                <button class="btn" type="submit">Edit</button>
              </form>
              <form method="post" action="/chats/<%= chat._id %>?_method=DELETE" style="display: inline;">
                <button class="btn" type="submit" style="background: linear-gradient(135deg, #e53e3e 0%, #c53030 100%); box-shadow: 0 2px 8px rgba(229, 62, 62, 0.3);" onclick="return confirm('Are you sure you want to delete this chat?')">Delete</button>
              </form>
            </div>
          </div>
          <div class="card-footer">
            <em>Tags: <i><%= chat.to %></i></em>
          </div>
          <div class="time">
            <p>
              <%= chat.created_at.toString().split(" ")[4] %>
            </p>
            <p>
              <%= chat.created_at.toString().split(" ").slice(0,4).join(" ") %>
            </p>
          </div>
        </div>
        <% } %>
      <% } %>
    </div>
  </body>
</html>
